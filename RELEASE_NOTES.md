# 동인메뉴판 v2.1.0

## 다운로드와 라이브러리 인식 안정화

- 기본 파일명을 `[작가][Hitomi ID] 제목` 형식으로 바꿔 긴 제목이 잘려도 서로 다른 작품이 같은 파일을 덮어쓰지 않도록 했습니다.
- 압축 파일을 `.part`로 완성한 뒤 최종 이름으로 전환합니다. 기존 파일은 덮어쓰지 않으며, 같은 Hitomi ID면 라이브러리에 다시 연결합니다.
- 다운로드 메타데이터를 책 기록에 직접 반영합니다.
- 이름이나 위치가 바뀐 파일을 Hitomi ID로 찾아 읽던 페이지, 읽음 상태, 즐겨찾기와 기록을 유지합니다.
- 실제 사본이 두 경로에 모두 존재하면 중복 파일로 유지합니다. 저장 장치에 접근할 수 없을 때는 이동으로 잘못 판단하지 않습니다.

## 상세정보와 다운로더 UI 통합

- 작품 상세, 시리즈 상세와 다운로더 미리보기의 메타데이터, 읽기 버튼, 경로 정보와 미리보기 구성을 일관되게 정리했습니다.
- 다운로더 미리보기에 Hitomi ID와 추가 메타데이터를 표시합니다.
- `male:`·`female:` 태그를 작품 상세, 시리즈 상세와 다운로더에서 같은 방식으로 구분합니다.
- 압축 중, 마무리 중과 다운로드 실패 상태 및 실제 오류를 명확히 표시합니다.
- 라이브러리 등록이 끝난 다운로드는 즉시 열 수 있습니다.

## 임시 파일과 썸네일 관리 개선

- 세 임시 저장 위치의 용도, 경로와 개별 크기를 표시합니다.
- 존재하지 않는 임시 폴더도 오류 없이 조회하고 정리합니다.
- 썸네일을 임시 결과로 먼저 생성한 뒤 안전하게 교체하고, 생성 과정의 임시 표지와 중간 파일을 정리합니다.

## 기타 개선

- GitHub Actions를 최신 버전으로 갱신하고 Node.js 실행 환경 경고를 정리했습니다.
- 다운로드 파일명 충돌, 완료 후 열기 누락, 라이브러리 미등록과 태그 표시 불일치 문제를 수정했습니다.

**전체 변경 내역**: [v2.0.0...v2.1.0](https://github.com/casm-bo/doujin-menu-V2/compare/v2.0.0...v2.1.0)
